<% @ingredients.each do |ingredient| %>
  <div class="row">
     <div class="col-md-4 col-sm-4">
      <p class="lead"><%= link_to(ingredient.name, ingredient) %>
      <p><%= ingredient.name%>
    
      <!--  TODO: Add code to display ingredients!å  -->
    </div>
  </div>
<% end %>